version: rules_v1
thresholds:
  schema_candidate_min: 0.45
  schema_high_priority: 0.70
  positive_decision_min: 0.72
  uncertain_lower: 0.50
  expanded_min: 0.55
  core_min: 0.80
  uncertain_min: 0.35

weights:
  agent: 1.0

stage_pruning:
  mode: relation_aware  # relation_aware | cross_stage_only | all
