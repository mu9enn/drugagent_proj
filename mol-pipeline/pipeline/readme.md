# Pipeline (Unified)

该目录是 `mol-pipeline` 的统一执行层代码：

- `claude_agent/`：执行并落盘 raw 会话
- `evaluate/`：`vs/ac/pf` 评测
- `postprocess/`：后处理与数据导出
- `e2e/`：E2E 适配
- `kg/`：KG-sampled 适配

请优先参考仓库根 `README.md` 获取最新入口命令。
